# Baked into `npm run build` only. Public URL - not a secret.
# Keep production API calls same-origin on Vercel so the Beacon.Auth cookie stays first-party.
# `frontend/vercel.json` rewrites /api/* to the Azure App Service backend.
VITE_API_BASE_URL=/api
