# Baked into `npm run build` only. Public URL — not a secret.
# HTTPS sites cannot call http://localhost (browser Private Network Access); production must hit a public API.
VITE_API_BASE_URL=https://intex2-a6d7e5dnave8hzd5.francecentral-01.azurewebsites.net
